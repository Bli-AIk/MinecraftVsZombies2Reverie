## Warning!

`Landing.unity` has the following changes in the **Reverie** project:

* `Landing(GameObject)` has been assigned the script **ReverieLandingSceneController**.

The purpose of this change is to load all **Reverie** Mod content.

## 警告！
`Landing.unity` 在 **Reverie** 项目中有以下更改：

- `Landing(GameObject)` 添加了脚本 **ReverieLandingSceneController**。

此更改的目的是加载 **Reverie** 的所有 Mod 内容。